import {
  capitalizeFirst, capitalizeLast, capitalizeByIndex, capitalizeByLetter,
} from './features/capitalize';

module.exports = {
  capitalizeFirst, capitalizeLast, capitalizeByIndex, capitalizeByLetter,
};
